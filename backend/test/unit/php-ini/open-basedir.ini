; Loaded only for the unit tests (see PHP_INI_SCAN_DIR in scripts/make/dev-test.mk).
; Some unit tests start helper processes (amphp) that need /dev/fd, which the production
; open_basedir forbids. Unit tests check program logic; the api tests run against the
; backend with the production configuration.
open_basedir =
