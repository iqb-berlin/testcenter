apiVersion: v1
kind: Service
metadata:
  name: broadcasting-server # !!! Hard coded in application as internal address !!! @TODO: Should be '{{ .Release.Name }}-broadcasting-server'
spec:
  selector:
    app: {{ .Release.Name }}-broadcasting-pod
  ports:
    - name: http-service
      port: 3000
      targetPort: http-container
