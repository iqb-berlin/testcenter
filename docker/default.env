# Basic settings

HOSTNAME=localhost
PORT=80
TLS_PORT=443

# Services

## Database connection
MYSQL_ROOT_PASSWORD=secret_root_pw
MYSQL_DATABASE=iqb_tba_testcenter
MYSQL_USER=iqb_tba_db_user
MYSQL_PASSWORD=iqb_tba_db_password
MYSQL_SALT=t

## Broadcasting Service
BROADCAST_SERVICE_ENABLED=true

## File Service
FILE_SERVICE_ENABLED=true

## Cache Service
### Allowed memory usage for cache in byte. 2147483648 = 2GB. Default is 1GB.
CACHE_SERVICE_RAM=1073741824
### Use the cache service to cache whole files (or only to cache authentification tokens for the files service)
CACHE_SERVICE_INCLUDE_FILES=off

# Network
### MTU Setting. Default is 1500, but Openstack actually uses 1442
DOCKER_DAEMON_MTU=1442
### Should TLS be used (prod setting)
TLS_ENABLED=no

## User ID settings (is set automatically from `make init` or `make fix-docker-user`)
UID=user_id_placeholder
GID=user_group_placeholder
