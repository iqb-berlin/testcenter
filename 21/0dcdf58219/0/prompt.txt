when switching into fullscreen via rootElem.requestFullscreen in frontend/src/app/test-controller/components/test-controller/test-controller.component.ts there is now a white strip at the top only on ipad, which cannot disappear no matter what you do in fullscreen. as a hint, but maybe not related, the same white strip at the top also appears on ipad when i swipe down to reload and after the reload i manually reload via browser button. the scroll bar on the right shows that the actual application without the white strip is not on the very top of the page. this was not in git release 17.6.0 before the vast ui changes in frontend.