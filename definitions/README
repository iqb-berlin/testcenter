This folder contains the relevant XSD-definitions for the XML-files which control the
Testcenter. The accompanying JSON-files which define run-modes, booklet-configurations
and such should be included into the XSDs once.